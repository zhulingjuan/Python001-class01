端口扫描器,常用端口(1-1024) 是否开放

多进程或多线程模

1， ping 命令快速检测一个 IP 段是否可以 ping 通，如果ping不通，或者网路异常
2， 指定 IP 地址开放了哪些 tcp 端口
3，终端显示该主机全部开放的端口
4，并发数量，由命令行参数传入
5，增加异常处理
6，指定并发数量

命令行参数举例如下：
pmap.py -n 4 -f ping -ip 192.168.0.1-192.168.0.100

pmap.py -n 10 -f tcp -ip 192.168.0.1 -w result.json


